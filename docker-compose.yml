version: '3.8'

services:
  assessment:
    image: mrksdh/assessment  
    ports:
      - "8080:8080" 

  nils:
    build:
      context: .  
      dockerfile: Dockerfile  
    image: nils:latest 
    depends_on:
      - assessment  
